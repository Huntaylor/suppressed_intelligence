build_runner:
  (aliases): [br]
  build:
    (aliases): [b]
    (command): dart run build_runner build --force-aot
    application: |
      cd packages/application
      ${{ build_runner.build }}
    data: |
      cd packages/data
      ${{ build_runner.build }}

barrel: barreler build
